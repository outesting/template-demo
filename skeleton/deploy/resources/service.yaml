apiVersion: v1
kind: Service
metadata:
  name: ${{ values.component_id }}
spec:
  selector:
    app: ${{ values.component_id }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000